#include <cstdio>
#include <cstring>
#include <algorithm>
using namespace std;
int main()
{
    int u, n, m, a, b, c, p, q;
    int f[] = {0,3,2,3,3,3,3,3,3,33,3,3,3};
    int g[] = {0, 31,28,31,30,31,30,31,31,30,31,30,31};
    int i, j, k, y, m, d, yy, mm, dd;
    while(scanf("%d", &n) != EOF)
    {
        for(m = 1; m <= n; m++)
        {
            scanf("%d %d %d %d %d %d", y, m, d, yy, mm, dd);
            
            printf("%d\n", s);
        }
    }
    return 0;
}