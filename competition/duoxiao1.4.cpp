#include <cstdio>
#include <cstring>
#include <algorithm>
#include <queue>
#include <cmath>
#include <vector>
#include <set>
#include <map>
#include <cctype>
using namespace std;
typedef long long ll;
const double eps = 1e-8;
const int MOD = 1e9 + 7;
ll N;
int main()
{
    ll i, j, k, u, n, m;
    int cas=1;
    while(scanf("%lld", &N) != EOF)
    {
        printf("Case #%d: %lld\n", cas++, (ll)(N*(log10(2.0))));
    }
    return 0;
}