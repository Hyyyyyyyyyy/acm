#include <iostream>
using namespace std;
int main()
{
    int i, j, k, n, m, a, b;
    while(scanf("%d", &n) != EOF)
    {
        if(n % 3)
        {
            printf("Kiki\n");
        }
        else
            printf("Cici\n");
    }
    return 0;
}