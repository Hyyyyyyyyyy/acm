#include <cstdio>
#include <cstring>
#include <algorithm>
#include <queue>
#include <cmath>
#include <map>
#include <vector>
#include <cctype>
#include <set>
using namespace std;
const int INF = 2147483640;
const double eps = 1e-7;
typedef long long ll;
const int maxn = 50000 + 100;
int main()
{
	int i, j, k, u, n, m, cas = 1, N;
	while (scanf("%d", &N) != EOF)
	{
		printf("Case #%d: %.0f\n", cas++, floor((double)N * log10(2.0)));
	}
	return 0;
}